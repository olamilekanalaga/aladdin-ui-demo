"use client";
import {WalletBalance} from "./WalletBalance";
import {useAladdinWallet} from "@/app/hooks/useAladdinWallet";
export function ConnectedWallet(){const wallet=useAladdinWallet();if(!wallet.connected)return <div className="connectedWalletPanel disconnected"><small>TRADING WALLET</small><b>Not connected</b><p>Intelligence remains available. Connect a wallet only when you want to use trading actions.</p><button type="button" onClick={()=>wallet.openWalletModal()}>Connect Wallet</button></div>;return <div className="connectedWalletPanel"><small>TRADING WALLET</small><b>{wallet.providerName}</b><code>{wallet.publicAddress}</code><WalletBalance refresh/><span className="devnetState"><i/>Solana Devnet</span><button type="button" onClick={()=>void wallet.disconnectWallet()}>Disconnect Wallet</button></div>}