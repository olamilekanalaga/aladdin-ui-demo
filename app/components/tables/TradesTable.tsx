import {trades} from "@/app/data/intelligence";import {wallets} from "@/app/data/wallets";import {behaviourColor} from "@/app/utils/behaviour";import {Badge} from "@/app/components/ui/Badge";import Link from "next/link";
export function TradesTable(){return <div className="dataTable"><div className="tr head"><span>TIME</span><span>BEHAVIOUR</span><span>WIN / SAMPLE</span><span>POSITION</span><span>AMOUNT</span><span>SIDE</span><span>SUPPLY</span><span>REMAINING</span><span>WALLET</span></div>{trades.map(t=>{const w=wallets.find(x=>x.id===t.walletId)!;return <div className="tr" key={t.id}><span>{t.time}</span><span><Badge color={behaviourColor[w.behaviour]}>{w.behaviour}</Badge></span><span>{w.winRate}% / {w.trades}</span><span>{t.position}</span><span>{t.amount} SOL</span><b className={t.side.toLowerCase()}>{t.side}</b><span>{t.supply}</span><span>{t.remaining}%</span><Link href={`/wallet/${w.id}`}>{w.short}</Link></div>})}</div>}
