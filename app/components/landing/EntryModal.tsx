"use client";
import {useEffect,useRef} from "react";
import {MessageCircle,Wallet,X} from "lucide-react";
import {useRouter} from "next/navigation";
import {useAladdinWallet} from "@/app/hooks/useAladdinWallet";
export function EntryModal({open,onClose}:{open:boolean;onClose:()=>void}){const router=useRouter(),wallet=useAladdinWallet(),closeRef=useRef<HTMLButtonElement>(null);useEffect(()=>{if(!open)return;closeRef.current?.focus();const key=(event:KeyboardEvent)=>{if(event.key==="Escape")onClose()};addEventListener("keydown",key);return()=>removeEventListener("keydown",key)},[onClose,open]);if(!open)return null;const simulated=(mode:"google"|"telegram")=>{wallet.beginAccount(mode);router.push("/launches")};return <div className="entryBackdrop" onMouseDown={event=>{if(event.target===event.currentTarget)onClose()}}><section className="entryModal" role="dialog" aria-modal="true" aria-labelledby="entry-title"><header><div><small>ALADDIN INTELLIGENCE</small><h2 id="entry-title">Continue to Aladdin</h2></div><button ref={closeRef} type="button" aria-label="Close entry options" onClick={onClose}><X/></button></header><button type="button" onClick={()=>simulated("google")}><b className="googleMark">G</b><span>Continue with Google<small>Local simulated account session</small></span></button><button type="button" onClick={()=>simulated("telegram")}><MessageCircle/><span>Continue with Telegram<small>Local simulated account session</small></span></button><div className="entryDivider"><span>or</span></div><button type="button" className="walletEntry" onClick={()=>{onClose();wallet.openWalletModal({entryAsWallet:true,afterConnectPath:"/launches"})}}><Wallet/><span>Connect Solana Wallet<small>Real Wallet Standard connection · Devnet</small></span></button><p>Account authentication and trading-wallet connection are separate. A wallet is optional until trading is requested.</p></section></div>}