import {useMemo} from "react";
export function WalletConstellation({address}:{address:string}){const points=useMemo(()=>{let seed=0;for(const c of address)seed=(seed*31+c.charCodeAt(0))>>>0;return Array.from({length:9},(_,i)=>{seed=(seed*1664525+1013904223)>>>0;const x=12+(seed%76);seed=(seed*1664525+1013904223)>>>0;const y=12+(seed%76);return{x,y,r:i===0?3:1+(seed%2)}})},[address]);return <svg className="walletConstellation" viewBox="0 0 100 100" role="img" aria-label="Deterministic Aladdin wallet constellation"><defs><radialGradient id={`g${address.slice(0,3)}`}><stop stopColor="#75d8ff"/><stop offset="1" stopColor="#9b68ff"/></radialGradient></defs>{points.slice(1).map((p,i)=><line key={`l${i}`} x1={points[i].x} y1={points[i].y} x2={p.x} y2={p.y} stroke="#7054a8" strokeOpacity=".28" strokeWidth=".7"/>)}{points.map((p,i)=><circle key={i} cx={p.x} cy={p.y} r={p.r} fill={`url(#g${address.slice(0,3)})`} opacity={i===0?1:.72}/>)}</svg>}