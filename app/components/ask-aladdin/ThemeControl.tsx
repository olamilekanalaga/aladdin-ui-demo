"use client";import {useEffect,useState} from "react";import type {ThemePreference} from "@/app/types/workspace";import {applyTheme,loadTheme} from "@/app/services/workspace-local";
export function ThemeControl(){const [theme,setTheme]=useState<ThemePreference>("dark");useEffect(()=>{const value=loadTheme();setTheme(value);applyTheme(value)},[]);return <select aria-label="Theme" value={theme} onChange={e=>{const value=e.target.value as ThemePreference;setTheme(value);applyTheme(value)}}><option value="dark">Dark</option><option value="light">Light</option><option value="system">System</option></select>}
