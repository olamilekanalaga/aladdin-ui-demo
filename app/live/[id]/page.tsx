import {tokenById} from "@/app/data/tokens";import {money,pct} from "@/app/utils/format";import {TokenChart} from "@/app/components/token/TokenChart";import {IfaNow} from "@/app/components/ifa/IfaNow";import {TradesTable} from "@/app/components/tables/TradesTable";import {Badge} from "@/app/components/ui/Badge";import Link from "next/link";
export default async function Live({params}:{params:Promise<{id:string}>}){const {id}=await params;const t=tokenById(id);return <><div className="tokenHero"><span className="coin large" style={{background:t.color}}>{t.symbol[0]}</span><div className="tokenName"><span>LIVE TERMINAL</span><h1>${t.symbol} <small>{t.name}</small></h1><p>7hkL...pump <button>Copy</button> · {t.age} old</p></div><div className="heroMetrics"><div><small>MARKET CAP</small><b>{money(t.mc)}</b><em>{pct(t.change)}</em></div><div><small>LIQUIDITY</small><b>{money(t.liquidity)}</b></div><div><small>24H VOLUME</small><b>{money(t.volume)}</b></div><div><small>HOLDERS</small><b>{t.holders}</b><span>+34 / 5m</span></div><div><small>MIGRATION</small><b>{t.migration}%</b></div></div><Badge>{t.formation}</Badge></div><div className="terminalGrid"><TokenChart/><IfaNow/></div><div className="investigate"><div className="tabs">{["Trades","Participants","Most Profitable","Largest Holders","First 100","Holders","Live State","Historical Match"].map((x,i)=><button className={i===0?"active":""} key={x}>{x}</button>)}</div><div className="tableHeading"><div><h2>Live evidence</h2><p>Wallet context and position state for the open token.</p></div><Link href={`/ask-ifa?context=token:${t.id}&label=${encodeURIComponent(`$${t.symbol}`)}`}>Ask IFÁ about ${t.symbol} →</Link></div><TradesTable/></div></>}
