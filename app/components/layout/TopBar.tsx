"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {Wifi} from "lucide-react";import {tokens} from "@/app/data/tokens";import {money} from "@/app/utils/format";
export function TopBar({showMarketData=true}:{showMarketData?:boolean}){const [time,setTime]=useState("");useEffect(()=>{const f=()=>setTime(new Intl.DateTimeFormat("en-GB",{hour:"2-digit",minute:"2-digit",second:"2-digit",hour12:false,timeZone:"Europe/London"}).format(new Date()));f();const i=setInterval(f,1000);return()=>clearInterval(i)},[]);if(!showMarketData)return null;return <><header className="topbar"><b>{time} <em>LDN</em></b><span className="live"><i/>SOLANA · LIVE</span><span>SOL <strong>$182.41</strong></span><span>RPC <strong>43ms</strong></span><span>BLOCK <strong>0.4s</strong></span><span className="systemMode">BEHAVIOUR BEFORE PRICE</span><Wifi size={14} color="#33d6a6"/></header><div className="ticker"><div>{[...tokens,...tokens].map((t,i)=><Link key={i} href={`/live/${t.id}`}><b>{t.migration===100?"MIGRATED":t.migration>70?"MIGRATING":"NEW"}</b> · ${t.symbol} · {t.age} · MC {money(t.mc)} · {t.buyers} buyers</Link>)}</div></div></>}