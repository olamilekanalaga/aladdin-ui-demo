import {answerModes,type AnswerMode} from "@/app/types/ask-aladdin";
export function ProfileGate({onSelect}:{onSelect:(mode:AnswerMode)=>void}){return <div className="profileGate" role="dialog" aria-modal="true" aria-labelledby="profile-title"><div><span className="eyebrow">PERSONALISE YOUR INVESTIGATION</span><h1 id="profile-title">How should Aladdin analyse evidence for you?</h1><p>Your profile changes prioritisation and language, never the underlying facts. You can switch answer mode for any question.</p><div className="profileChoices">{answerModes.map(mode=><button key={mode} onClick={()=>onSelect(mode)}><b>{mode}</b><span>{mode==="Trader"?"Market state, flow, retention and risk":mode==="Analyst"?"Lifecycle, cohorts, anomalies and quality":"Sustainable participation, trust and communication"}</span></button>)}</div><small>Profile preference saved only in this browser.</small></div></div>}
